#!/usr/bin/env ruby

args = ARGV.first.split.map do |item|
  next item unless item == '%s'
  ARGV[1..-1].map { |f| f =~ / / ? f.inspect : f }.join ' '
end

puts args.join ' ' if ENV['FORWARD_DEBUG']

exec *args
